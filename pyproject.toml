[project]
name = "sc-spectral-analysis"
version = "0.1.0"
description = "Spectral analysis scripts for Lax-Wendroff schemes"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.9",
    "numpy>=2.0",
    "scipy>=1.14",
]

[tool.uv]
package = false